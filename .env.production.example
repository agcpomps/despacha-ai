# Copia para .env no servidor: cp .env.production.example .env
# Usado pelo docker-compose.prod.yml

# o teu domínio, sem https:// (o Caddy trata do certificado)
DOMAIN=despachaai.com

DB_USER=despacha
DB_PASSWORD=muda-isto-para-uma-password-forte
DB_NAME=despacha_ai

# gera com: openssl rand -base64 48
JWT_SECRET=muda-isto

# número que recebe os pedidos de destaque (formato +244XXXXXXXXX)
PUBLIC_ADMIN_WHATSAPP=+244900000000
